"""apt2 的配图机位。

⛔ 每个坐标都从 layout 算出来，**不写裸数字**（house2 立的规矩）：
   户型一改，机位跟着走，不会出现"图还是老的、屋子已经变了"。

⚠️ `make_docs_images.render_eye` 只有 yaw、**没有俯仰**。想往下看只能用 `EXTERIOR`
   （它是 pos + target 的形式）。

⭐ 本场景多一类镜头 `POSED`：把机器人摆成 layout 的 `SIT_POSES` 再**推物理到稳态**才拍。
   ⛔ 别改用 EYE / EXTERIOR 拍那张——产物里的机器人站在世界原点，静态渲染拍不到"坐着"。
"""
from __future__ import annotations

from scenes import manifest as _SCENES

_L = _SCENES.load_layout("apt2")

DOG_EYE = 0.38          # 四足机器狗的眼高
HUMAN_EYE = 1.25        # 人形机器人的眼高（G1 实测）

_AX = _L.ENFILADE_X                                    # 贯通轴线
_GR = _L.ROOMS["great_room"]["rect"]
_GR_C = ((_GR[0] + _GR[2]) / 2.0, (_GR[1] + _GR[3]) / 2.0)
_GAL = _L.ROOMS["gallery"]["rect"]
_DIN = _L.ROOMS["dining"]["rect"]
_KIT = _L.ROOMS["kitchen"]["rect"]
_MEZ = _L.ROOMS["mezzanine"]["rect"]
_STAIR = _L.ROOMS["stair_f62"]["rect"]
_F1 = _L.FLOOR_Z(1)
_CX, _CY = (_L.X0 + _L.X4) / 2.0, (_L.Y0 + _L.Y3) / 2.0
_SOFA = _L.GR_SOFA_XY

# ── 俯视 ────────────────────────────────────────────────────────────────
# ⚠️ 复式的两层平面是**重叠**的，纯俯视只能拍到最上面那层（house2 早就立过这条，
#    它三层也只留一张整体俯视）。⛔ 所以这里**不做"分层俯视"**——那是拍不出来的，
#    硬做只会得到两张几乎一样、还说不清是哪层的图。楼内情况靠下面的站位镜头看。
# ⭐ 唯一的例外正好是本场景的看点：双高客厅那一块上层没铺楼板，
#    所以俯视能**一直看到 62 层的沙发组**——图上那块深色人字拼地板就是。
TOPDOWN = [
    ("A1", "A1-户型俯视图.png", (_CX, _CY), 30.0,
     "整体俯视（拍到的是 63 层）：⭐ 中间那块深色人字拼是双高客厅的挑空，"
     "透过它一直看到 62 层的沙发与茶几"),
    ("A2", "A2-楼梯间俯视.png", ((_STAIR[0] + _STAIR[2]) / 2.0, (_STAIR[1] + _STAIR[3]) / 2.0),
     10.0, "双跑楼梯：楼层平台 → 上行跑 → 中间平台 → 回头跑，中间一道梯井隔墙"),
]

EYE = [
    ("V1", "V1-贯通轴线-从入户门望公园.png", (_AX, _L.Y0 + 0.9, HUMAN_EYE), 90,
     "★ 贯通轴线：玄关 → 画廊 → 双高大客厅，尽头是中央公园上方的空气"),
    # ⚠️ 标题只写这张图**真拍得到**的东西。`render_eye` 只有 yaw、没有俯仰，
    #    站在地面平视是看不见"双高"的——那个交给下面的 X3。
    ("V2", "V2-大客厅-三开间落地窗.png", (_GR_C[0], _GR[1] + 0.9, HUMAN_EYE), 90,
     "★ 大客厅正对三开间落地窗：中央公园 + 两侧天际线，单椅与茶几都是真碰撞资产"),
    # ⚠️ 站在桌子侧后方（第一版）只拍得到一个椅背和半张桌面。退到房间南端、
    #    正对长桌轴线往北拍，六把椅子才全在画面里。
    ("V3", "V3-餐厅-六把真碰撞餐椅.png",
     ((_DIN[0] + _DIN[2]) / 2.0, _DIN[1] + 0.5, HUMAN_EYE), 90,
     "★ 餐椅走 CoACD 真碰撞：椅腿之间是通的，不是一块实心砖"),
    ("V4", "V4-厨房中岛.png", ((_KIT[0] + _KIT[2]) / 2.0, _KIT[1] + 1.0, HUMAN_EYE), 90,
     "厨房中岛与操作台，西北转角窗"),
    ("V5", "V5-楼梯间-从楼下往上看.png",
     (_STAIR[0] + 0.8, (_STAIR[1] + _STAIR[3]) / 2.0, HUMAN_EYE), 0,
     "62 层楼梯间：24 踢面/层的双跑楼梯，梯井竖着通到 63 层"),
    ("V6", "V6-上层环廊.png", (_MEZ[0] + 1.0, _MEZ[1] + 0.8, _F1 + HUMAN_EYE), 60,
     "63 层挑空环廊：右下那道白色实心栏板之外，就是 3.75 m 下去的双高客厅"),
    ("V7", "V7-主卧转角窗.png", (_L.X0 + 2.4, _L.Y3 - 1.8, _F1 + HUMAN_EYE), 135,
     "63 层主卧：西北转角两面落地窗"),
    ("V8", "V8-狗视角-画廊.png", (_GAL[0] + 1.0, (_GAL[1] + _GAL[3]) / 2.0, DOG_EYE), 0,
     "画廊展线，机器狗视角"),
]

# ⚠️ EXTERIOR 是唯一能俯视的镜头形式（pos + target）
EXTERIOR = [
    ("X1", "X1-越过窗框俯瞰公园.png", (_GR_C[0], _L.Y3 - 1.8, 2.00),
     (_GR_C[0], _L.Y3 + 500.0, -_L.ELEV),
     "★ 从 232 米俯瞰中央公园 —— 俯角是几何算出来的，不是照片贴的"),
    ("X2", "X2-本楼外景-两层.png", (58.0, -46.0, 30.0), (0.0, 0.0, 2.0),
     "从外面看这两层：层间实腹带 + 屋顶女儿墙，再往下是 232 米"),
    # ⭐ 这是唯一一张真能看出"双高"的图。相机悬在**挑空正上方**（63 层标高、但在环廊北侧
    #    的空档里）往南下方看，一眼同时看到：下层的沙发组、上层环廊的实心栏板、以及两者之间
    #    那 3.75 m 的空。
    # ⛔ 别把相机放在环廊上往北拍（第一版就是）：栏板 1.05 m 实心，平着看正好被它挡死；
    #    更早那一版还把相机放到了 y=−1.8（那是南带的健身房），拍出来是一堵墙。
    ("X3", "X3-双高客厅-挑空.png", (_GR_C[0], _GR[3] - 0.9, _F1 + 1.85),
     (_GR_C[0], _GR[1] + 1.7, 0.70),
     "★ 双高大客厅：相机悬在挑空里往下看 —— 下层沙发组、上层环廊栏板、中间 3.75 m 的空"),
]

# ⭐⭐ 姿态镜头 —— 本场景存在的理由就是这一张
#    (标签, 文件名, SIT_POSES 的 key, 用哪台机器人, 相机位置, 看向哪, 说明)
POSED = [
    ("S1", "S1-机器人坐在沙发上.png", "gr_sofa", "g1",
     (_SOFA[0] - 2.8, _SOFA[1] + 2.6, 1.15), (_SOFA[0] + 0.1, _SOFA[1] - 0.15, 0.62),
     "★★ G1 坐在客厅沙发上：大腿水平、小腿竖直、脚掌踏平地板（座面 0.35 m）"),
    ("S2", "S2-坐姿-正面.png", "gr_sofa", "g1",
     (_SOFA[0] + 0.05, _SOFA[1] + 3.1, 1.05), (_SOFA[0], _SOFA[1] + 0.1, 0.70),
     "同一坐姿的正面：背靠靠背，前面是茶几"),
]
