"""house2 的配图镜头清单。

镜头是**场景专属事实**（站在哪、看哪儿只对这一栋楼成立），所以跟着场景走，
不放在生成脚本里——house1 同理，见 `scenes/house1/shots.py`。

⚠️ 眼高必须和 `robots/manifest.py` 里那台机器人**实际**的相机高度对得上，
否则配图说"人形看到的"其实是个不存在的机位。

这栋楼的重点是楼梯，所以镜头以"能不能看清楚梯段是连着的"为准来选：
从底层望上去、站在梯段中间、站在休息平台、以及到达二层回望。
"""
from __future__ import annotations

DOG_EYE = 0.38      # 四足机器狗头部相机高度(m)
HUMAN_EYE = 1.25    # 人形 G1 头部相机高度(m)，装在 torso_link 上

STOREY = 2.80       # 与 layout.STOREY_H 一致；下面的绝对高度按它推

# ── 俯视：(编号, 文件名, 看向哪, 视野距离m, 说明) ──
# ⚠️ 三层的平面是重叠的，纯俯视只能拍到最上面那层。所以这里只留一张整体俯视
#    （拍的是顶层），楼内情况靠下面的站位镜头看。
TOPDOWN = [
    ("A1", "A1-三层楼-顶视.png", (0.0, 0.0), 26, "整栋俯视（关掉天花板，看到的是顶层）"),
]

# ── 站位：(编号, 文件名, 站在哪(x,y,z), 朝向角度, 说明) ──
#    朝向：0=朝 +x(东)，90=朝 +y(北)，180=朝西，270=朝南
#    ⭐ 这一栏比 house1 多一个 z：三层楼必须说清站在第几层。
EYE = [
    ("S1", "S1-底层望向楼梯.png", (3.34, 0.20, HUMAN_EYE), 90,
     "0 层，站在门口正对上行梯段（门是对着梯段开的，进门就能上）"),
    ("S2", "S2-站在上行梯段中间.png", (3.34, 1.90, 0.53 + HUMAN_EYE), 90,
     "踩在上行梯段中部，看向休息平台"),
    ("S3", "S3-休息平台回望.png", (4.30, 3.80, 1.40 + HUMAN_EYE), 270,
     "站在半层高的休息平台，回望上来的那一跑与回头跑"),
    ("S4", "S4-二层到达平台.png", (4.90, 1.60, STOREY + HUMAN_EYE), 270,
     "1 层到达平台上，朝出口方向——脚下这块局部楼板就是不掉下去的原因"),
    ("S5", "S5-二层回望楼梯.png", (4.90, 1.20, STOREY + HUMAN_EYE), 90,
     "1 层到达平台上回望刚爬上来的回头跑"),
    ("S6", "S6-三层到达平台.png", (4.90, 1.60, 2 * STOREY + HUMAN_EYE), 270,
     "2 层到达平台，同样的结构再来一遍"),
    ("R1", "R1-底层客厅.png", (-2.00, 2.40, HUMAN_EYE), 180, "0 层客厅"),
    ("R2", "R2-二层主卧.png", (-2.00, 2.40, STOREY + HUMAN_EYE), 180, "1 层主卧"),
    ("R3", "R3-三层工作间.png", (-2.00, 2.40, 2 * STOREY + HUMAN_EYE), 180, "2 层阁楼工作间"),
]

# ── 外景：(编号, 文件名, 相机位置, 看向哪, 说明) ──
# 用"给定机位 + 看向某点"，不是绕目标转的自由相机语义（那套容易把相机放进墙里）。
EXTERIOR = [
    ("X1", "X1-整栋外景.png", (16.0, -13.0, 9.0), (0.0, 0.0, 3.5), "整栋三层外景"),
]
