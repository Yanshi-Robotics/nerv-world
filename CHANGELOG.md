# alice-house Changelog

## [Unreleased] — 2026-09-02 策略迁出

- ⛔ **`policies/` 整目录移出本仓**。训练好的运动策略是训练产物，不是场景资产；现由消费方的策略发布架保管
  （`policy.onnx` + `contract.json` + `release.yaml`）。本仓从此只交付场景与机器人本体。
- `robots/manifest.py` 删去 `policy_dir` / `turn_vx` / `command_ranges` / `lin_cmd_deadband` / `pd_mode`——
  这些是策略属性，随策略一起迁到 `release.yaml`。清单保留模型路径、出生高度、相机、跟拍机位、`foot_bodies`。
- `tools/make_docs_images.py` 的坐姿沉降改从 `ALICE_HOUSE_CONTRACT_JSON` 读契约（缺了响亮报错，不猜）。
- README / AGENTS 同步。消费方参考从 anima-zero 改指 NERV。

场景资产库的版本记录。**要点：保持简洁，每版只说重点。**（格式参考 [Keep a Changelog](https://keepachangelog.com)）

## [0.15] — 2026-08-22

Main: **家具有了和真家具一样的碰撞，机器人真的坐得下**——外加新场景 **apt2**
（62–63 层复式顶层豪宅，双高客厅 + 双跑楼梯）。

- **真碰撞（`decor/hulls.py`）**。根因不是"没开碰撞"：`convert.py` 按**材质**拆部件，
  而 MuJoCo 对碰撞网格只取**凸包**——实测凸度餐椅 0.30 / 单椅骨架 0.12 / 圆茶几 0.22，
  所以把 `contype` 打开只是把实心盒换成实心凸包。改用 **CoACD 凸分解**（离线、seed 固定、
  凸块不入库）：餐椅俯视高度图从「整个占地一律 97 cm」变成「座面 43–46 / 靠背 83–96」，
  同一条座下横射线，apt1 的单椅把它挡在 0.721 m，apt2 的餐椅让它一路穿过去、15.25 m 外
  才撞到书架。⭐ 整套房子的代价：**apt1 3329 geom / 15.7× 实时 → apt2 3719 geom（含 184 个
  碰撞凸块）/ 14.1× 实时**，约 10%。`nmeshgraph` 与件数无关（网格按资产共享），
  所以 6 把同款餐椅只声明一套凸块。
- ⛔ **凸块必须带 `solref="0.005 1"`**：默认接触太软，3.9 kg 从 1.20 m 砸下会**穿过
  49 mm 厚的座面凸块**。⚠️ 减时间步没用、加 `margin` 也没用（都实测过）。
  典型的「编译不报错、慢速测试全绿」静默坑，为它单列了一道门。
- ⭐ **座面高上限 0.36 m**，从 G1 腿长实测出来（小腿 0.318 + 踝高 0.033）：
  坐姿保持 3 秒，0.30 ✅ / 0.35 ✅ / 0.40 ❌ 滑落 / 0.45 ❌ 直接倒。
  ⇒ 沙发用新的 `furniture.sofa()` **手写基本体**（座面 0.35 / 进深 0.45），⛔ 不用真网格——
  `_fit_scale` 只做均匀缩放，压座面会把整件缩成玩具。
  顺带一个意外结论：**市面上的真家具对 G1 全都太高**（Poly Haven 餐椅 42.3–46.1 cm、
  单椅 53.0–70.9 cm），所以它们有真碰撞但不进「能坐」名单。
- **新场景 apt2**：24 空间 / 690 ㎡。窗外景色**直接引用 apt1 的**（⛔ 没重跑
  `make_view.py --nyc`），外轮廓与 apt1 逐位相同。竖向推导方向和 house2 **相反**且必须如此：
  层高被 apt1 的 `FLOOR_TO_FLOOR=3.75` 钉死，所以固定层高、反推踢面高，取 24 踢面/层
  → `STEP_RISE=0.15625`、坡度 27.51°。
- **姿态出图（`scenes/apply_pose.py` + `POSED` 镜头）**：产物里的机器人站在世界原点，
  静态渲染拍不到"坐着"。坐姿按**关节名**声明在 layout 的 `SIT_POSES`（⛔ 不按下标），
  装配后**推 3 秒物理到稳态**再拍，并把判据（骨盆漂移 / 滑移 / 倾角 / 接触数）随图打印。
- 新门三道，各做过故障注入：`check_collision_solref`、`check_seat_reachable`
  （座面高 + 座上净空 + **按件可选**的座下净空）、以及 `check_decor_inside_box` 认凸块那条路。
  ⚠️ 座下净空**默认不查**是想清楚之后的选择：人坐下时小腿竖直、脚落在座面前沿之外，
  整体软包到地的沙发照样坐得住，给它开这一条会判出假红。
- ⭐ 老的 `check_decor_ray_invariance` **不用改**：实测 180 条射线里第一个命中是纯视觉网格的
  次数为 0——凸块把网格整个裹住了，包含性不变式原样成立，只是容器从一个盒子换成 N 块凸包。
- ⛔ 订正 `AGENTS.md` 一条旧红线：「include 外来 MJCF 会改 `nu`/`nq`/`nv` 弄坏策略契约」——
  前半（撞 body 名、`<option>` 静默覆盖、`<default>` 撞名）照样成立，
  但「`nu` 一变就坏」**实测不成立**：消费方是按关节名与传动目标建映射的。
  apt2 加了 184 个碰撞 geom，`nu` 仍是 29。

## [0.14] — 2026-08-09

Main: **apt1 有了一天**——早晨 / 白天 / 黄昏 / 夜晚四套光照，
外加 README 场景图鉴大修与两道新验收门。

- 四时段是**运行期预设**（`scenes/apply_time_preset.py`）：产物逐字节不变（白天即产物），
  晨/昏/夜在已加载的模型上改写灯光、材质自发光、天空盒与立面贴图。
  黄昏 = 上西区隔公园染金、西窗金光扫进主卧；夜晚 = 夜间立面几千扇窗随机点亮、
  室内暖色灯岛、中央公园压成黑洞。天空盒 `make_view.py --sky-phase` 分时段出；
  夜空程序化生成（曼哈顿的夜空是被城市照亮的橙灰，野外 HDRI 拍出来像沙漠）。
- ⛔ 实测发现并写进验收门：MuJoCo 渲染器只点亮 **headlight + 7 盏模型灯**
  （`mjMAXLIGHT=100` 是容量不是能力）——apt1 此前 11 盏里有 4 盏从来没亮过。
  新门 `check_lights_render`（灯数超预算当场红）与 `check_time_presets`
  （预设静态自洽：只准引用产物里已有的实体、玻璃 alpha 不许为 0 等）。
- 地毯改纯视觉（`walkover`）：盲策略踩上 1.6 cm 毯盒当场步态崩坏（实测 5 秒只挪 0.35 m），
  脚踩地板、毯只管看。产物里新增一只具名 `film` 相机（消费方逐帧改写位姿拍片用）。
- README（中英）：新增「apt1 的一天」（四时段对比图）与「逐间看」（三条带 + 书房/主卫新配图
  V9/V11）；出图命令补上 `ALICE_SCENE`（旧写法照抄只会出默认场景）；版本节从 v0.8 追平。

## [0.13] — 2026-08-08

Main: **入户门终于看得见、柜子转正、装饰网格有了纹理、竖直面不再是条纹。**
Jeff 走进 apt1 报了四处，查下去只有**四个根因**，而且都不在他指的那几件家具上。

| 他看到的 | 真正的根因 |
|---|---|
| 玄关只有一面白墙 | 门板**整块埋在墙体里**（三个场景错了两个半） |
| 柜子又黑又乱、结构像穿透 | ① 它**正面顶着墙**；② 网格没 UV |
| 门洞里一个怪箱子 + 床头一个没渲染的盒子 | 网格没 UV —— 那"箱子"是**床**，"盒子"是**床头柜** |
| 沙发像竖条纹不像布 | 贴图是 `type="2d"`，MuJoCo 在基本体上**沿局部 Z 投影**，竖直面被拖成条纹 |

### ⛔ 入户门埋在墙里

生成器的墙**从房间矩形的边往屋内长满一个墙厚**，而三个 layout 都把门心放在矩形边上、
又各自手填了一个"比墙薄"的厚度 → apt1/house1 两面都看不见，house2 只朝外。
射线实测：从玄关朝门打六条，**全部命中墙，一次都没打到门**。

→ 改成**生成器画的一组构件**（门扇 + 三面门套 + 竖向长拉手），
⭐ **厚度由生成器从 `WALL_THICK` 推**（比墙厚、两面各凸 2 cm），layout 不再有机会填错；
写法与 `WINDOWS` 一致（room/side/center/width/height）。门套再凸 3 cm，那圈凸边才是
"一眼认出是门"的关键。apt1 的两扇私人电梯门同病，一并挪出墙面。
新闸门 **`check_front_door_visible`**（射线，故障注入三场景全红）。
⭐ 教训：`check_no_dead_declarations` 只问"进没进产物"——**"在产物里"不等于"看得见"**。

### ⛔ gr_console 正面顶墙

三只同款木柜只有它靠**东**墙，却抄了另两只靠西墙的 `yaw=90`。网格正面是局部 −y
（两法验证：柜门面板外表面贴 y=−0.250 紧邻极值；小部件重心偏 −y 38%）→ 改 `yaw=-90`，
离墙净空 2.7 mm → 2.3 cm。新闸门 **`check_mesh_faces_room`**。
⚠️ 只查 `decor/manifest.py` 里**显式声明 `front`** 的资产（目前仅 console）：
同一套启发式对油烟机会判错（它的小部件是烟道，本来朝墙）。⛔ 宁可少管，不要瞎猜。

### ⛔⛔ 39 张装饰网格没有 UV

`decor/convert.py` 的 `export_obj` 把 `include_texture` 写成 False。那个开关管的**不是
"要不要打包贴图"**（那是 `write_texture`），而是**要不要写 `vt` 行**。没有 UV，MuJoCo 就按
**uv=(0,0) 采一个像素**涂满整件：console 那像素是 (49,31,17) 近黑棕、bed/nightstand 是
(192,191,187) 灰白。⭐ 锁死根因的证据：没 UV 的**正好是**走 convert.py 的 16 件，
而走 `decor/robocasa.py` 的 4 件电器全都有 UV。

→ 改 True + `decor.fetch --force`（联网重下 110 MB）+ `decor.calibrate`。
⭐ **上游没变**：16 件的尺寸与面数与重下前**逐位相同**（sha256 变了只因多了 vt 行）。
带 UV 的网格 28/67 → **66/67**（剩一张是没贴图的拉手）；渲染标准差 0 → 24.9（柜）/24.9（床）/21.6（床头柜）。
新闸门 **`check_mesh_has_uv`**（材质引用了贴图却没 texcoord 就红）。

### ⛔ 竖直面的条纹：贴图类型，不是 texuniform

MuJoCo 给**基本体**贴 `type="2d"` 纹理时沿几何体**局部 Z 轴投影**——只有水平面对，
四个竖直面被沿 Z 拖成条纹。地板一直好看、沙发/墙面一直像拉丝金属，就是它。
→ 方形贴图一律改 `type="cube"`（六面各自投影）。
⚠️ **走过的弯路**：`texuniform` 治不了这个，实测开/关渲染**逐像素一样**——它管的是
"texrepeat 相对 geom 还是按空间单位"。塔楼立面之所以正常，是它本来就声明成了 cube。
⚠️ 更值得记的是：`scenes/apt1/layout.py` 里**早就写着**"竖直面必须用 cube，这个坑本仓
已经栽过两次"，**而紧接着的九张贴图一个 `type` 都没写**。知识写下来了没落到代码上，
这是第三次栽。
⛔ 两类必须留 2d：整图只铺一次只看一面的（城市底图、挂画）、**非正方形的图**
（cube 要求 PNG 是 gridsize 整数倍，`appliance_panel` 512×128 会编译期报错）。
判据**从图片实际尺寸算**，⛔ 不手维护名单。
实测沙发各向异性 **9.10:1 → 3.97:1**。

### 顺手

`decor/fetch.py` 的 `_write_attribution` 无条件取 `r['author']`/`r['tris']`，
而 RoboCasa 那 4 件是 `robocasa.py` 直接写进 lock 的、没这两个字段 →
**每次 fetch 都在写完 lock 之后崩在 KeyError 上**，署名表一直没更新；
而且屏幕先打完整张成功汇总表才抛异常，很容易被当成"跑完了"。
⚠️ 更要命：那 4 件的出处说明是**手写贴在这个"自动生成"文件末尾**的，
fetch 一旦跑通就会被整段抹掉——**而它承载的是 CC-BY 要求的署名**。已改成容错 + 生成。

## [0.12] — 2026-08-08

Main: **走廊清空、床转正、机器人有了自己的房间**，外加一道新闸门
`check_reachability` —— 它一上来就照出**三处谁也没发现的死区**。

### ⛔ 三个静默失效（都是 Jeff 自己走进去撞见的，48 项自检全绿）

1. **`check_door_passable` 跳过 `kind="open"` 的通道。** 而开敞洞口恰恰最容易被家具顶死——
   它没有门框拦着。apt1「画廊→大客厅」那个 4.00 m 开口被沙发组塞掉 3.76 m、只剩 0.44 m，
   **G1 只能到达大客厅 2%**（0.52/37.8 ㎡），北半区 10.4 ㎡ 连落地窗带茶几整块是**孤岛**——
   正是作品集封面那个机位。→ 删掉那行 `continue`。
2. **生成器从来没把机器人搬到出生点。** 三个场景的机器人都站在**世界原点**：
   apt1 撞进画廊长凳 22.7 cm、house1 撞进冰箱和两面墙（96 个接触点），
   house2 纯粹因为原点恰好是空地才看着正常。
   ⭐ 摆位本来就是**运行期**的事（消费方 anima-zero 一直是自己写 qpos 的），
   现在 `walkthrough.py` 也照做，摆到新增的停机位。
3. **apt1 主卧的床反了 180°。** 网格床头板在 y=5.44（北），而两个床头柜在 y=3.55（南）——
   **床头柜摆在了床尾**，那只"床尾凳"贴在床头后 25 cm。加一个 `yaw=180` 解决。
   自检里**没有任何一项管家具的朝向**，只管位置、碰撞、通行。

### ⭐⭐ 新闸门 `check_reachability`

判据不是"两件家具之间多宽"，而是**整层的可通行区域只准有一个连通块**。
⛔ 逐缝报数会被床头柜↔床、餐椅↔餐桌、贴合橱柜炸出上百条假阳性，而且**根本没法自动判断
哪条缝该管**——沙发和茶几之间 0.42 m 是正常客厅，绕得开就没事。可通行性直接回答"绕不绕得开"。

算法：房间净空 ∪ 门洞 → 扣掉挡路家具（精确 OBB，⛔ 不用保守外接框）→ 两遍 chamfer 距离变换 →
取 dt ≥ 机器人半宽 → 连通域标记。纯 stdlib，三个场景 0.4 秒。

⚠️ **第一版判据写错过一次，是故障注入当场打脸的**：原来写"每间屋只要有一个可站立格子在主
连通块里就算过"，而堵死的大客厅南缘仍留着一条窄带连着画廊，于是报绿。
⭐ 这正是本仓那条老教训的又一次现形：**查"存在一条路径"证明不了"这地方走得通"**。

新闸门照出的死区（全部已修，都是"挪一件家具"而不是删）：

| 场景 | 死区 | 堵点 |
|---|---|---|
| apt1 | 大客厅北半区 11.06 ㎡ | 沙发组顶死唯一入口 → 沙发+抱枕+茶几+木碗**整组北移 0.20** |
| apt1 | 大客厅西侧 1.27 ㎡ | 落地灯站在 1.09 m 走道正中劈成 0.33+0.30 → 贴西墙 |
| house1 | 客卫淋浴间 1.74 ㎡ | 玻璃 x∈[-5.35,-3.15] 对房间净空 x∈[-5.36,-3.14]——**墙到墙一整面，淋浴间根本没有门** → 收窄到 1.10 靠西摆，东侧留 0.95 m |
| house1 | 次卧过道 0.42 ㎡ | 书桌椅"拉出来"卡在过道入口 → 塞回桌下 |

另有 house1 次卧**算术上就摆不下**（1.30 床 + 0.60 桌 + 0.60 人 = 2.50 > 净宽 2.22）——
这是三处里唯一必须动房间尺寸的：客卫/次卧隔墙西移 0.35，床、淋浴托盘、玻璃、马桶水箱、
客卫窗全部同轮跟随。

### 其余

- **走廊不放落地家具**（Jeff 定）：删掉画廊长凳 `gl_bench`（就是撞机器人那只）与东过厅长凳
  `eh_bench`。⚠️ 衣帽间中岛 `dr_island` **留着**——它在封闭房间里，不是走廊。
- **新增 layout 契约字段 `ROBOT_HOME_XY` / `ROBOT_HOME_YAW`**（「保姆间」，多层再加 `ROBOT_HOME_FLOOR`）。
  apt1/house1 选洗衣房、house2 选二层储藏间：都有门直通主流线、空地够、且**不在必经动线上**。
  ⛔ 和 `START_POS_XY` 有意分开：那是**任务出生点**（消费方读它跑导航），这是**停机位**。
- **apt1 出生点原来正踩在地毯上**，而 `layout.py` 自己的注释写着「⛔ 不许踩在地毯上」——
  注释和代码自相矛盾了整整一版。地毯北移 0.40，出生点与英雄镜头一个字没改。
- `ROBOT_CLEARANCE` 补上**扫地机器人**（0.349 m，来自 open-cleaning-robot 的 params.xacro）。
  ⚠️ 判据值**没变**（仍是 G1 的 0.60，实测三台肩宽/机身宽 0.446 / 0.317 / 0.349）。
  ⛔ 同轮发现那张表被四处当成了"本仓有哪些机器人"，加一台没模型的当场去找 `apt1-cleaning.xml`——
  已拆成 `_modelled_robots()`。
- 新增 `BODY_TOP_M = 1.35`：吊柜、油烟机、挂画不再被当成落地障碍。
- **鼠标灵敏度减半 + 改成与分辨率无关**：单位从"度/像素"改成"横扫满窗口转多少度"（默认 77°），
  换屏幕不再变快；新增 `--mouse-sens` 微调。
- 顺手：house1 的 `START_YAW` 原来写的是截断的 `3.14159`，改 `math.pi`。

## [0.11] — 2026-08-08

Main: **隐身碰撞盒的 geom 分组 3 → 4，apt1 因此终于能被消费方挂起来。**
除了这一个数字，apt1 的几何、材质、尺寸、贴图**一个字节都没动**（验证办法见下）。

### 修复

- **apt1 在 anima-zero 的 `sim-house-nav` 世界里起不来**（`:8112` 启动即 `ValueError`）。
  根因是一次**语义撞车**：`group` 在 MuJoCo 里只管"画不画"，但消费方还得靠它把
  「机器人自己」和「房子」分开——它的激光测距要滤掉打在自己身上的射线，而 `mj_ray`
  的过滤接口**只吃一个 6 位分组掩码**，没法按 geom 或 body 子树过滤。
  于是它按 Menagerie 惯例认定 2/3 归机器人；而 [0.9] 起 apt1 的 31 个隐身碰撞盒正好占着 3。
  house1 / house2 只用 0/1，所以一直没暴露。
  → 隐身组改用 **4**，提成具名常量 `make_house.HIDDEN_BOX_GROUP`，
  并把完整的分组归属表写进 `AGENTS.md`（房子只准用 0/1/4/5，硬上界 ≤ 5）。

### 怎么验证"只改了那一个数字"

把 [0.10] 的产物和新产物各过一遍 `sed 's/group="3"/group="GRP"/; s/group="4"/group="GRP"/'`
再 diff：**两份产物都零差异**。原始 diff 恰好 31 行，形状全是 `group="3"` → `group="4"`；
`dm_` 装饰网格计数仍是 154，天花板 `group="1"` 仍是 13。
house1 / house2 的产物未重新生成，逐字节不变。

## [0.10] — 2026-08-08

Main: **一次纯粹的重组** —— 目录归位 + 一次场景改名。**任何几何、材质、尺寸都没有动。**
产物除了多一层 `../` 路径前缀之外与 [0.9] **逐字节相同**（验证办法见下）。

### ⛔ BREAKING（四条，按对消费方的影响排序）

1. **`scenes/manifest.py` 的 `scene_filename()` 现在返回带目录的仓根相对路径**
   `build/<场景>-<机器人>.xml`，原来是裸文件名。
   ⭐ 按文档那样写 `os.path.join(<资产库仓根>, scene_filename(...))` 的消费方
   **一个字都不用改**（已实测）；自己拼 `f"{scene}-{robot}.xml"` 的会断——
   那本来就是文档里 ⛔ 过的用法。⛔ 函数名不改：改了就是一次跨仓破坏，名字给兼容性让路。
2. **场景 key `house3` → `apt1`。** 第三个地方是曼哈顿 62 层的**公寓**而不是房子，
   从它开始按类型命名。⛔ `house1` / `house2` **不改名**——最早 build 的两个，留纪念
   （建造顺序写进了 README）。配图目录随之 `docs/images/house3/` → `docs/images/apt1/`，
   场景目录 `scenes/house3/` → `scenes/apt1/`。
   ⚠️ 贴图目录 `textures/house3/` 与 `h3_` 材质前缀**有意保留**：那 9 张材质的文件名
   本身就叫 `h3_oak.png`，只改目录会造出新的不一致；而 `nyc_massing.py` 是
   `make_view.py --nyc` 从 NYC Open Data **联网抓出来的生成物**，为改名重跑它
   会拿到可能已更新的数据——那就不是改名，是换内容。定性为"这个场景的资产命名空间"。
   ⚠️ `docs/house4-设计草案.md` → `docs/apt2-设计草案.md`（[0.9] 里提到的旧名不改，
   历史按当时的事实记；目录历史用 `git log --follow` 看）。
3. **7 个入口脚本搬进 `tools/`**：`python make_house.py` → `python tools/make_house.py`。
4. **产物 XML 搬进 `build/`**；`furniture.py` → `scenes/furniture.py`，layout 里改用
   `from scenes import furniture as F`；`robots/*/*.xml` 的 `meshdir` 各加一层 `../`。

### 怎么验证"只改名、只挪位置"

- 改名那一步（产物还在仓根）：`cmp house3-g1.xml apt1-g1.xml` **逐字节相同**。
  产物里 `<mujoco model="sim_house_nav_g1">` 不含场景 key，19 处 `house3` 全是贴图路径。
- 挪进 `build/` 那一步：把 [0.9] 的产物过一遍
  `sed -E 's/( file[a-z]*=")(textures\/|decor\/|robots\/)/\1..\/\2/g'` 再 diff，
  6 份**全部零差异**。⭐ 那条 sed 碰不到 `file="../../../decor/…"`（引号后是 `../`），
  所以它**同时证明了 mesh 前缀该保持不变**。

### 新增的两道闸门

- **`_assert_meshdir_agrees()`**：同一个 meshdir 现在在两处各存一份，**语义有意不同**
  （`robots/manifest.py` 存仓根相对的 durable 事实，机器人 XML 存从产物目录看的形式）。
  这个仓被"同一个量存两处、没人逼它们对账"咬过两次，所以生成每份产物时当场读 XML 核对。
  ⚠️ 它第一版挂在 `_decor_prefix()` 里，而那个函数只有**有装饰网格的场景**才会调到——
  实测把 meshdir 改错后 house1/house2 照常生成成功，漏检三分之二的产物。已挪到无条件路径。
- **`make_house.py --out` 的落点闸门**：产物里的 `../` 层数按"仓根下恰好一层"算死了，
  落到别的深度 XML 长得一模一样、写文件也照常成功，只有 MuJoCo 加载时才报错。

### 顺手修掉的三个既有问题

- `check_lock_reconciles` 用 `if key != SCENES.keys()[0]` 实现"只跑一次"，是**拿排序当
  控制流**：① 场景一改名首个 key 就换人，"对账在哪个场景上跑"悄悄挪了地方；
  ② `--scene <非首个>` 时它一次都不跑，屏幕上却显示 ✅。改成显式的模块级标志。
  ⚠️ 行为变化：`--scene house2` 从"静默跳过"变成"真跑"，只会更严不会放水。
- `make_docs_images.py` 的 `OUT_DIR` 被赋了两次，前一次是死代码——搬家时最可能的错法是
  改了死的那行、以为改完了，配图写到 `tools/docs/images/` 而**脚本照常打印完成**
  （这个仓 2026-07-26 栽过一模一样的跟头）。已删。
- `check_scene` 的贴图存在性检查原来按仓根解析，那是在这里复制一份 MuJoCo 的规则；
  改成**相对产物 XML 自己的目录**，产物再挪也不用动它。
- `.gitignore`：`preview/` 与 `output/` 两条删掉——仓里根本没有这两个目录、也没有脚本
  写它们，而 `output/` 那条**无前导斜杠**，和它上面"`scenes/**/output/` 是交付物要保留"
  的注释**正好相反**。`build/` 显式反选（很多人的全局 gitignore 会忽略它）。

### 给消费方的话

anima-zero 侧**无需任何改动**（已用三个 key 各实测一次）；只有
`world/sim-house-nav/scene_assets.py` 里那句报错文案还写着老的 `python make_house.py`，
不影响功能，等下次动那个仓时顺手改。

## [0.9] — 2026-08-07

Main: **家具的尺寸终于是对的。** 上一版有三个叠在一起的缩放 bug，让全屋真网格家具被缩小
1.0–4.2 倍（床变成 0.56×0.68×0.26 m 的玩具床还悬空 29 cm），而 **48 项自检一条都没红**。
自检 48 → **19 项 × 3 场景**，其中 4 项是新加的，专门守这次这类静默失效。

1. **⛔⛔ 根因一：`decor/calibrate.py` 少转了一次旋转。**
   MuJoCo 编译 `<mesh>` 时不只把顶点平移到重心，**还会旋转到惯性主轴系**（量在 `mesh_quat`）。
   老代码只把 `mesh_pos` 加了回去，于是写进 lock 的包围盒是"轴被置换过"的：
   床记成 `0.66×2.21×2.15`（真值 `1.69×2.06×0.78`）、条案记成 `1.31×0.81×2.44`
   （真值 `2.44×0.52×0.68`）。回转体（花瓶、胸像）转了也一样，所以"大部分看着都对"，
   越不对称的错得越离谱。
2. **⛔⛔ 根因二：摆位多加了一份重心。**
   MuJoCo 把 `mesh_pos/mesh_quat` **抄进 `geom_pos/geom_quat` 自己补偿掉了**——
   实测 `<geom type="mesh" pos="0 0 0">` 的世界包围盒与 OBJ 文件包围盒**逐位相同**。
   老代码用 `lock.part_offset()` 又加了一遍，多部件资产的每个部件因此往外飞自己的重心那么远
   （条案两条柜腿各飞 ±0.49 m）。**这才是 0.8 里条案和床头柜被自检判红的真因**，
   跟"网格不闭合""残差叠加"没有关系。→ 删掉 `decor/lock.part_offset()`。
3. **⛔ 根因三：`_fit_scale` 把 yaw 算了两遍。**
   `item["size"]` 是碰撞盒**局部系**的边长，而盒子本身就带着同一个 yaw 的 quat，
   网格用同一个 quat 转——比例关系与 yaw 无关。老代码额外按"旋转后 AABB"估外廓，
   餐椅因此白缩 23%。0.8 里写的依据"旋转后 AABB 会低估"在数学上不成立。
4. **⭐ 三个补丁常数全部删除，因为它们的理由都不存在了**：`_FIT_EPS_MULTI` 与
   `_multi_margin()`、`_FRIDGE_SHRINK`、`mesh_piece(shrink=)`。
   `_FIT_EPS` **0.88 → 0.99**，并且这次是实测定标的：设成 1.0 重新生成后全场 25 件装饰的
   最坏超出是 **+4.84 µm**（纯 `%g` 六位有效数字的截断），0.99 给最薄的木碗留 0.50 mm。
   **结果：全场缩放系数从 0.24–1.03 回到 0.99–1.07。**
5. **⭐ 四项新自检**（每项都做了故障注入验证：注回旧 bug 当场变红、拔掉回绿）：
   - **`check_lock_reconciles`** —— lock 里各部件 `offset±half` 的并集必须等于整件 `size`。
     **这就是让根因一活下来的那道没人记的账**；上闸门时 20 件资产里 15 件对不上（最大差 176 cm）。
     纯 stdlib，不要 mujoco 也不要资产字节，裸 clone 上照样能红。
   - **`check_decor_inside_box`** —— 直接从产物读顶点，量网格在碰撞盒局部系里探出多少毫米。
     ⛔ 它不能被射线不变性取代：**那条打的全是水平射线**，只证伪得了水平方向的探出。
   - **`check_door_passable`** —— 门两侧各取一点必须在房间里 + 门前后 0.6 m 的**净通行宽**
     ≥ 机器人宽。上闸门时抓到 3 处"柜子沿墙一拉到底、正好压死那面墙上的门"，全部修掉。
   - **`check_furniture_not_through_wall`** —— 家具不许捅穿墙伸进隔壁。抓到 3 件。
     ⚠️ 判据是"穿到墙的另一面"而不是"碰到墙"：把柜子画到房间矩形边、和墙贴死不留缝
     是本仓的建模惯例（三场景合计 56 件），按"碰到就红"会把惯例判成 56 个 bug。
6. **⛔ 修好 `check_decor_ray_invariance` 里 `_inside()` 的一个假阳性**：它判断
   "射线起点在不在家具盒子里"时**没有把点变换到盒子的局部系**，于是把一只 yaw=90 的
   2.44 m 条案当成东西向的，柜子内部的采样点被当成自由空间。
7. **⭐ 三处条案/餐边柜换上真木柜网格**（Poly Haven `modern_wooden_cabinet`，18 MB
   在磁盘上闲置了一整版）。⚠️ 三个碰撞盒统一成网格自己的比例 `_CREDENZA = 2.44×0.52×0.68`
   再用 yaw 转到墙上——缩放按三轴最紧的一比取值，盒子比例一偏就白缩
   （旧的玄关条案 `0.38×1.40×0.84` 实测只能填到 57%）。柜面高变了，柜上三只花瓶跟着落。
8. **⭐ 床头柜复活**（@elba `Tumb Astrid`）——它当年被撤的理由是根因二，一个字符串就改回来了。
   **木碗的碰撞盒从 0.20×0.20×0.11 放大回网格真尺寸** 0.313×0.309×0.093，
   那个收窄同样是为了迁就 bug 写的。**至此 20 件资产全部用上，一件不剩。**
9. **⭐ 画廊那株"三个绿球拼的"盆栽换成真发财树**（Poly Haven `pachira_aquatica_01`，CC0）。
   ⚠️ 0.8 撤它的判断（"包围盒 6.87 m 宽，带了多余的 LOD 实例"）是错的：
   上游把**四棵不同大小的树并排摆在一个文件里**。→ 新增 `mesh_piece(..., parts=(3, 7))`
   部件白名单，`_asset_span()` 按子集求并集并把中心减掉，**摆位自动归正**，
   不用去 Blender 切、也不用为每棵树各建一个 lock key。
   `parts=None` 时代码路径逐字节不变——**house2 的两个产物这一版完全没动**，就是证据。
10. **⭐ `walkthrough.py` 的探路射线从一根变三根** `(0.35, 0.70, 1.00)`。
    以前只有一根 1.0 m 的胸口射线，**矮于 1 m 的家具全都不挡人**——茶几 0.50 / 长凳 0.44 /
    餐桌 0.75 / 床 0.82 / 台面 0.92 —— 走一圈下来会误以为"这些柜子没有碰撞"。
    ⚠️ **场景的碰撞一直是对的**（家具全是 `contype=1`），错的是漫游工具的探路方式。
    两端边界是**算出来的**：下界 > 一帧探程内台阶最多升的 0.32 m，上界 < 半层平台处的墙顶
    1.29 m。selftest 里新增一条断言守着这两个数——实测 0.31 就会卡在楼梯上。
11. **新增 `docs/house4-设计草案.md`**：62–63 层复式 + 室外泳池平台的设计与工作量评估。
    ⛔ 只有文档，`scenes/house4/` 未建。

## [0.8] — 2026-08-06

Main: **house3 从"结构对、但全是盒子"变成真东西**——真实天空、2716 栋真实纽约建筑、
真实航拍公园、CC0 照片级室内材质，以及**真家具网格**（Poly Haven CC0 + Objaverse CC-BY
+ RoboCasa CC-BY 的厨房电器）。自检 37 → **48 项，全绿**。

1. **⭐ 天空：Poly Haven `kloofendal_48d_partly_cloudy_puresky`（CC0）的实景六面立方图。**
   ⭐ 它有**现成的 8 位色调映射 JPG**，不用解 Radiance、不用自己 tonemap
   （MuJoCo 只吃 8 位 PNG/KTX，这一步本来躲不掉）。
   ⛔ **横向旋转 180°**：南非拍的，南半球正午太阳在北，不转会在中央公园正上方挂一个
   40.77°N 不可能出现的太阳。
   ⛔⛔ **六个面到世界方向的对应是实测出来的，而且全是反的**：相机朝 +X 看到 `L` 面、
   朝 +Z 看到 `D` 面——up↔down、left↔right 全部对调。照文档字面命名填，天空会上下翻转+左右镜像，
   **而且渲染出来照样很好看**。映射表写死在 `make_view.SKY_FACE_FOR_DIR`，改之前先重跑 `--calib`。
2. **⭐ 2716 栋真实曼哈顿建筑**（NYC Open Data `5zhs-2jue`，Local Law 11：无使用限制、
   **无 share-alike**）。⭐ 只存包围盒不存多边形——场景已按街网转正 29.05°，
   **旋转后轮廓的 AABB 就是它的有向包围盒**。
   ⚠️ 高度必须 `ground_elevation + height_roof`（曼哈顿地面标高差 26 m，看得出来）；
   ⛔ 过滤条件必须含 `'Merged'`，否则**丢掉 111 West 57th**（它继承 1924 年 Steinway Hall 的记录）。
   ⛔ 不用 OSM 当主源：覆盖率相当，但 2716 栋的坐标表提交进 MIT 仓构成 ODbL 的"派生数据库"。
   ⭐ 交叉验证：手写的 12 栋塔楼高度**就是**这份数据的 `height_roof` 英尺值 ×0.3048
   （中央公园大厦 1550 ft = 472.4 m，逐位吻合）。
3. **⭐ 室内换成 ambientCG 的 CC0 照片级材质**（9 张，`fetch_assets.py` 下载 + SHA-256 记账）。
4. **⭐⭐ `build()` 的发射顺序倒过来（室内在前、窗外在后）** —— 这是 maxgeom 溢出的安全阀。
   实测：MuJoCo 按 geom 在模型里的**先后顺序**填渲染缓冲、**不按距离**，缓冲满了
   **只打一条 warning、不报错**。窗景排在前面时溢出，**近处 200 件家具一个都进不去（0/200）**——
   也就是"公寓凭空消失、窗外风景完好"。倒过来后最坏情况降级成"远处少几栋楼"。
   ⛔ 而且 `maxgeom` **不能在 MJCF 里声明**（schema violation），它是调用方参数，
   **场景文件保护不了自己**。
5. **四条新自检**，逐条故障注入验证过：`check_wellformed`（合法 XML）、
   `check_assets`（材质/贴图存在 + 天空盒恰好一个）、
   ⛔ `check_geom_budget`（按最严的消费方默认值 10000 断言）、
   `check_void`（朝外的房间往外必须撞到**可碰撞**的东西，⚠️ 要查 `geom_contype != 0`，
   否则打到装饰件会假通过）。
6. **⭐ 三处贴图朝向修正**（同一个根因的第三、四、五次）：塔楼立面改 `type="cube"` 并用
   `gridlayout` **给顶面单独的屋顶贴图**（从 62 层往下看，矮楼全戴着窗格当屋顶）；
   墙面加 `WALL_FACE_FIX` 开关把竖墙转到"局部 +Z = 法线"（house1/house2 默认关，逐字节不变）。
7. **自检抓出的真错误**：本楼原本放在离公园 120 m 处（约 57 街），
   **和公园之间隔着 59 街整整一排真楼**。`check_park_sightline` 拿真实建筑数据当场报出来，
   改成 34 m（中央公园南路路宽），本楼就贴着公园南沿了。
8. **⭐ 室内材质换成 ambientCG 的 CC0 照片级贴图**（9 张，`fetch_assets.py` 下载 +
   SHA-256 记账 + `--verify`）。⚠️ ambientCG 用 403 拒默认的 `Python-urllib` UA，要带标识。
   ⚠️ 换上之后**墙面立刻露出竖条纹**——还是那条"2D 贴图沿局部 Z 投影"的规则，
   **这是它第三次咬人**（前两次是城市背景板和塔楼立面）。house1/house2 没露，
   是因为它们的程序化噪点图条纹读起来像拉毛墙面。
   先试 cube 贴图，但细长的墙板上会呈**放射状**（cube 是从物体中心打射线取样的）；
   最终用"把墙转到局部 +Z 朝法线"，加 `WALL_FACE_FIX` 开关只对 house3 生效。
9. **⭐ `decor/` 装饰网格管线**（登记表 / 可执行的许可闸 / GLB→OBJ 转换 / 减面 / 自动署名）。
   网格是**纯视觉外衣**，碰撞仍由原来的 box 承担。
   ⭐ 实测确认 `contype=0` **且** `conaffinity=0` 时 MuJoCo **完全跳过 qhull**：
   同一份产物去掉装饰再比，`nmeshgraph` 417372 → 417372，**5 张网格一个凸包都没建**。
10. **⭐⭐ 新自检 `check_decor_ray_invariance`**：装饰不许改变**任何**射线读数。
    做法不是去藏装饰（藏不掉，`mj_ray` 不看 contype），而是**证明它不影响射线**——
    同一批采样点打两遍（`bodyexclude=-1` vs 排除 `decor_visual`），要求逐位相同。
11. **⚠️ 管线里踩到并修好的坑**（每一个都不报错）：
    - trimesh **不做 Y-up→Z-up**（浴缸会躺着）；
    - **多物体 OBJ 在 MuJoCo 里静默损坏**（只取第一块的面、却取全部的贴图坐标）；
    - `fast-simplification` 要 **float32 顶点 + int32 面**，喂 float64 报的是
      `Buffer dtype mismatch`，而且**只有真正触发减面的资产才会撞上**——面数少的一路绿灯，
      很容易误判成"管线是通的"；
    - 减面**会丢 UV**，必须走 `return_collapses` + `replay_simplification` 自己重映射；
    - trimesh 的 glTF 加载器用**下标**访问 resolver 而不是 `.get()`；
    - ⛔ **最隐蔽的一个**：`parts()` 起初给**每个材质组各自居中**，多材质资产会**散架**
      （扶手椅的坐垫和框架各自被挪到原点）；同时 lock 里记的尺寸取自 `sc.extents`、
      和导出的 OBJ 对不上，结果碗在场景里**立了起来**。
      两处都改成"整件统一居中 + 尺寸取导出产物的包围盒"。

12. **⭐ 家具真的穿上外衣了 —— 但差点白做**：碰撞盒是**不透明**的，网格缩在它里面
    就被整个盖住。渲染不报错、48 项自检全绿，**只有看图才发现"上了真家具还是一堆白盒子"**。
    ⛔⛔ 而修法只能是 `group="3"`，**绝不能把 rgba 的 alpha 设成 0**：
    实测 alpha=0 之后 `mj_ray` **直接跳过这个 geom**——碰撞盒被悄悄挖空、导航和雷达全变，
    而编译照样不报错。是射线不变性自检当场抓到的（3 条射线穿过茶几打到了后面）。
    `group` 只管画不画（默认渲染器只画 0–2 组），射线一律照打。
13. **⛔ 包含性收口 —— 在这上面白转了六轮**。网格总探出碰撞盒，我先去**放大碰撞盒**，
    六轮都没收敛。根因是 `_fit_scale` 会把网格**按比例一起撑大**，超出量原封不动：
    **放大盒子对这个问题完全无效**。真正的三个原因是：
    - MuJoCo 编译 `<mesh>` 时按**重心**把顶点平移到原点，而管线记的是**包围盒中心**
      （实测最大差 312 cm）。→ 新增 `decor/calibrate.py`，从编译后的 `mesh_pos`
      **实测**重心写回 lock。
    - lock 记的包围盒取自导出**之前**的 trimesh 对象，和落盘的 OBJ 差最多 14%。
      → 同一个标定脚本顺带用**编译后的真实顶点**重算 `offset`/`half`。
    - 多部件资产往往**偏心**，按"关于原点对称"摆就会探出一侧。→ 摆位时减掉真实跨度中心。
    三条都修完，"重心 vs 包围盒中心"的最大偏差从 **312 cm 降到 24.9 cm**。
    > ⚠️ **这段的因果解释在 0.9 被推翻了**（结论"别放大盒子"仍然对）。第一条只说对了一半：
    > MuJoCo 还会把顶点**旋转**到惯性主轴系，漏掉那次旋转才是根因；而"生成器摆位改用重心"
    > 是错的——MuJoCo 自己已经补偿掉了，再加一次就是双重施加。见 [0.9] 第 1–2 条。
14. **⭐ 阶段 F：RoboCasa 厨房电器移植器 `decor/robocasa.py`**
    （NVIDIA HuggingFace 镜像，**CC-BY-4.0**；冰箱 / 四眼灶 / 水槽 / 抽油烟机）。
    ⛔ **不 `<include>` 那份 MJCF**：它带 `<actuator>`（灶具 8 个 motor）、`<joint>`、
    顶部的 `<option timestep gravity>`（会静默覆盖本模型的积分器）、重名的 `<default>` 类，
    而且**每件电器的根 body 都叫 `object`**，搬两件就撞名。
    ⭐ 移植器只取**视觉网格 + 贴图**，写成本仓 decor 资产的形式——
    于是电器直接走 `mesh_piece`，包含性缩放 / 标定 / 射线自检**全部自动生效**，
    一行新的摆位代码都没写。
    **验收：移植前后 `nu=29 nq=36 nv=35` 与 house1 逐位相同**，动作空间没被动过。
    ⚠️ 代价：门和抽屉打不开，灶具的 `<site>` 没搬。要做操作任务时再单独接。
15. **撤下的东西**（自检说不行就不放行，不留"看着还行"的余地）：
    条案（`modern_wooden_cabinet`，3 组）与床头柜（@elba `Tumb Astrid`，5 组）
    的网格外衣**撤回成盒子**——各组残差叠加，余量收到 0.78 仍有一组探出，
    再收就把柜子缩得不像话了。要复活得先在 Blender 里合成闭合网格再重拓。
    > ⚠️ **这个诊断在 0.9 被证伪了**：探出的真因是摆位多加了一份重心，跟网格闭不闭合无关。
    > 两件都已在 0.9 复活，**一行 Blender 都没动**。见 [0.9] 第 2、7、8 条。


## [0.7] — 2026-08-05

Main: **新场景 house3（曼哈顿高层豪宅大平层，窗外中央公园）+ 修掉两个从没被发现的老 bug**。
两个 bug 都属于"生成器不报错、自检全绿、截图看着还行，但产物其实是错的"，
所以每修一个就配一条自检把这一类挡住。

### house3 —— 第三个场景

23.0 × 15.0 m 满层一户，13 空间约 345 ㎡，层高 3.30 m，**62 层（窗台离街面 232.5 m）**。
house1 考平层导航、house2 考爬楼，house3 考的是**窗外**：视觉信息极强，而且窗外是 232 米空气。

1. **⭐ 窗景按距离分四层，切点是算视差算出来的**（120 m 处走 8 m 位移 = 61 像素，
   600 m 处 = 12 像素，3000 m 处 = 2.4 像素 → 600 m 以内必须真几何）：
   - **A 无限远**：天空盒（目前程序化渐变）
   - **B 120–600 m**：12 栋点名的塔楼（中央公园大厦 472.4 m、111 W57 435.3 m 等，
     高度是公开事实并逐条注明；平面位置按曼哈顿街网估算，已如实登记不是测绘坐标）
     + 公园东西两侧程序化生成的 80 栋街墙
   - **C 脚下 232 m**：⭐ **水平**贴图板（中央公园航拍 + 城市底图）——
     这是 MuJoCo 给基本体贴 2D 图**唯一投影正确**的朝向，而且**俯角由几何天然保证**，
     是任何一张平视照片都给不了的
   - **D 本楼外皮**：立面 + 窗台挑檐，否则这套房子在外景里是飘在天上的
2. **⭐ 公园与城市用的是真实航拍**——USGS NAIP 正射影像，**公共领域**（美国联邦作品，
   17 U.S.C. §105 不受版权保护），源像元 0.3 m，抓下来是 1.07 m/像素。
   ⚠️ **必须旋转 29.05°**：曼哈顿街网偏东约 29°，公园在正射影像里是斜的，而场景里是正南北。
   旋转角由公园南北界中点的经纬度算出（4132 m / 29.05°），和 layout 里手写的
   `PARK_L=4110` / `VIEW_BEARING_DEG=29.0` 互为交叉验证。出处与加工登记在
   `textures/house3/ATTRIBUTION.md`。
   ⚠️ 服务硬上限 4000 px，超了会返回**错误 JSON 而不是图片**，PIL 报的
   `UnidentifiedImageError` 完全看不出是尺寸超限——已在代码里显式挡住并给出人话报错。
3. **⭐ 塔楼贴上了窗格立面**（之前是纯色盒子，远看是一堆灰乐高）。
   ⛔ 必须用 **`type="cube"`** 贴图：2d 贴图在竖着的基本体上会被沿局部 Z 拉成条纹——
   本仓的城市背景板 v0.6 正是栽在这（见下面「两个陈年 bug」第 2 条）。cube 六面各自正确映射。
4. **`make_view.py`** 还留了不联网的程序化兜底：地物位置全部由真实街号推算
   （The Pond / Sheep Meadow / The Lake / Great Lawn / 水库 / Harlem Meer 各在正确的街区），
   地平线渐隐**烤进贴图外圈**（⛔ 不用 MuJoCo 的 haze/fog——那是渲染标志不是模型状态，
   每个消费方都得各自设，一定有人漏）。另带 `--calib` 出天空盒六面标定图。
5. **⛔ 落地窗有可碰撞的玻璃**（17 块）。没有它，`walkthrough` 的胸高射线打不到东西，
   人和机器人会直接**从 232 米走出去**——而仿真本身很开心。
6. **生成器新增可选钩子**，全部 `getattr` 守卫，house1/house2 产物逐字节不变：
   `SKYBOX` / `TEXTURES_EXTRA` / `MATERIALS_EXTRA` / `GROUND_SLABS` / `SKYLINE` /
   `HOST_TOWER` / `LIGHTS` / `VISUAL` / `STATISTIC`，窗支持逐扇 `top`（落地窗顶到天花板）。
7. **踩到并修好的四个坑**（都只有渲染出来才看得见，已全部写成注释留在代码里）：
   - ⚠️ `zfar × extent` 必须大于最远视景几何：500×6=3000 m 而公园伸到 4230 m，
     远半截凭空消失，**画面上读起来像"大气雾霾"**，根本想不到是裁剪。现为 1600×6=9600 m。
   - ⚠️ MuJoCo 的**非方向光默认 `cutoff=45°`**——是聚光灯不是全向光。
     十盏 45° 的锥子照不满 23×15 m，整层发暗，而人的第一反应会是"加灯"，越调越糊。
   - ⚠️ 本楼立面第一版比玻璃还往外凸 1.2 m，从窗边往下看整个画面被自家的墙堵死。
   - ⚠️ 把 220 Central Park South 放到了 y=+45，也就是**放进了公园里**，
     它正杵在大客厅和公园之间挡死左半边视野。
   ⭐ 后两条现在由新自检 `check_park_sightline` 兜住（本楼到公园之间不许有塔楼 +
     `zfar × extent` 必须盖得住最远的视景几何）。它上线后**立刻又抓出第三个**我肉眼没发现的：
     城市底图板 20000 m 而 zfar×extent 只有 9600 m，远角仍在被裁；顺带发现板子尺寸
     和 NAIP 实际抓取范围（16000 m）对不上，贴图一直是被拉伸的。已把两者收成同一个常量。

### 两个陈年 bug

⚠️ **本版会改变 house1/house2 的产物和配图**，消费方 anima-zero 的 `sim-house-nav`
导航碰撞盒朝向也跟着变（详见下面第 1 条）。

Features:

1. **⛔ 家具朝向全是错的（修）。** `<compiler angle>` 是**整个编译模型全局**的，而它来自
   `<include>` 进来的机器人 XML（`robots/g1/g1.xml` 写着 `angle="radian"`）。但
   `furniture.py` 的 yaw 单位是**度**，`_p()` 直接把它塞进 `euler` —— 于是
   `euler="0 0 90"` 被当成 **90 弧度**读。实测：本该 180° 的椅子实际是 **−126.76°**，
   本该 ±90° 的是 **±116.62°**。house1 有 70 个 geom 中招。
   反证是楼梯扶手一直是对的——`_stair_rails` 传的是 `math.atan2` 算出来的弧度，正好对上。
   **修法：旋转一律改出 `quat`**（四元数没有单位，换机器人、上游改 `angle` 都不会再错）。
   新增 `make_house.yaw_quat()` 与 `_rot_attr()`；`_box()` 同时支持 `quat`/`euler` 但禁止同时给。
2. **⛔ 窗外的城市背景板是竖条纹（修）。** MuJoCo 给**基本体**贴 `type="2d"` 纹理时没有 UV，
   是**沿几何体自己的局部 Z 轴投影**的。城市背景板是竖着的板（局部 +Z 是那根 34 m 的竖轴），
   大面正好投不上，2048×768 的天际线图从来没被看见过——一直显示成竖蓝条纹。
   ⭐ 本仓的地板之所以一直是对的，正因为地板是水平的。
   **修法：把板转到"局部 +Z = 法线"**（`_FACE_NS` / `_FACE_EW` 两个四元数），
   局部 X 变成板的水平方向、局部 Y 变成竖直方向。墙上挂画同理。
3. **⛔ `_wall_arts()` 是死代码（修）。** 它从写出来那天起就没被 `build()` 调用过，
   house1 声明的 11 幅挂画一幅都没进过产物。现已接上（house1 +22 geom）。
4. **⭐ 新自检 `check_no_dead_declarations`**：layout 里声明了东西，产物里就必须找得到。
   这是第 3 条那一类腐化的通用防线——生成器不会为此报错，只有拿声明去比产物才抓得到。
   已做故障注入验证：把 `_wall_arts()` 的调用去掉，它当场变红并指到根因。
5. **⭐ 新自检 `check_art_clear`**：挂画不许压在门洞/窗洞上。
   接上 `_wall_arts()` 之后立刻暴露出 **house1 有 4 幅画压在窗户和门洞里**（已改位），
   house3 自己也踩了一次（一幅画挂到贯通轴线的门洞正中，渲染出来是块大黑板挡死视线）。
   这类"两份声明各自合法、凑在一起才错"的问题，只能靠算重叠来抓。同样做了故障注入验证。
6. **`_furniture_geom` 未知 type 直接报错**。以前会**悄悄按 box 出**——打错一个字就是
   屋里凭空多一块板，而且从截图上根本看不出来。
7. **README 的 License 一节改成四层**：代码 MIT / 本仓自制资产 MIT / 第三方资产各带各的许可 /
   **本仓不再分发**的第三方数据集由用户自取。附一段说明为什么这个仓不改成非商用许可
   （简言之：真正诱人的那几个数据集是**合同**禁止再分发，改自己的许可解决不了；
   而 NonCommercial 会传染到渲染图）。

## [0.6] — 2026-08-02

Main: **多场景 + 第二个场景 house2（三层小楼，两组可通行楼梯）**。
house1 是单层大平层，没有任何高差，人形的爬楼、跨层导航、"摔在楼梯上"这些一个都测不到。

⚠️ **这一版反转了 [0.4] 的一个决定**：那次主动把 `domus01/` 拍平到仓根，判断是
"一个世界里有多个地方"而不是"多套户型并列"。Jeff 2026-08-02 拍板改回 CARLA 式的
多场景，`scenes/manifest.py` 的文件头把这段来龙去脉写清了，并留了一句⛔别再拍平。

Features:

1. **`scenes/<key>/layout.py` + `scenes/manifest.py`**，与 `robots/manifest.py` 成对：
   一边"有哪些地方"，一边"有哪些身体"，正交交叉组合。产物改名
   `house-<机器人>.xml` → `<场景>-<机器人>.xml`，命名规则的单一真相源在清单里，
   生成器与消费方都调它。**重构后 house1 的产物逐字节不变**。
2. **house2**：三层、11 空间、两组**按真实结构做的** U 型双跑楼梯。
   一部双跑楼梯是**五段、其中两段是平台**：楼层平台 → 上行跑 → 中间休息平台 →
   回头跑 → 上一层的楼层平台。⛔ 两块平台缺一不可，且**每一段的末级踏板与下一段的
   起始平台首尾相接、正好差一个踢面**——楼梯的连续性是算出来的，不是对齐出来的。
   ⭐ 一跑的**踏板数 = 踢面数 − 1**（最上面那一级就是平台本身，行业标准算法）。
   尺寸照 GB 50096-2011 §6.3：踢面 0.16、踏面 0.30、梯段净宽 1.20、平台进深 1.40、
   梯段净高 2.63；层高 2.88 m 由楼梯反推（`2 × 9 × 0.16`），不是先定层高再配楼梯——
   前作那栋两层小楼正是反过来做的，顶步悬空 20 cm 直到滚球验证才发现。
   两跑之间是一道 0.12 m 实心隔墙而非空槽（空槽正好卡机器人的脚）；顶层梯口加实心栏板；
   每级单独给摩擦系数，因为"上楼滑不滑"是想扫的变量。
   推导与规范出处见 `scenes/house2/楼梯设计.md`。
3. **`check_scene.py`**：这个仓第一套自动检查，**查产物、不查声明**。
   ⭐ 两项最值钱：`check_route` 沿**整条上楼路线**（楼层平台→上行跑→中间平台→回头跑→
   上一层→门口）每 4.7 cm 打一次射线；`check_joints` 逐个核四个接头。
   它抓到的错没有一个是看截图能发现的：`no_floor` 声明了但生成器从没实现（梯井被地板
   封死，而查声明的检查报绿）、梯井还需要 `no_ceiling`、净空按半个墙厚算导致第一级埋进墙、
   材质名 `mat_concrete` 根本不存在、门槛底下 0.14 m 的楼板裂缝、顶层梯口没拦住的洞。
   ⛔ **开发中最贵的一次**：回头跑的起点写在了梯井北墙根而不是中间平台南缘，两跑根本没接上
   （从平台看过去是一排凌空的板子），而当时**七项检查全绿**——它们只沿单独一跑打射线，
   绕着走确实摸得到一条路。**查"存在一条路径"证明不了"这是一部楼梯"。**
   上面两项就是补这个洞的，并用故障注入验证过：把起点改回北墙根，两项当场变红。
4. **生成器支持多层**：每间屋一个 `floor` 键 + `FLOOR_Z(n)`；`no_ceiling` 与 `floor_rects`
   成对开竖井；楼梯怎么走由 `stair_route(floor)` 给出**唯一**一份权威描述，
   自检与漫游都照它走。单层场景这些都没有，偏移恒为 0，house1 产物逐字节不变。
5. **`walkthrough.py`**：第一人称漫游（WASD + 鼠标，`T` 透视、`F` 飞行、数字键跳层），
   带碰撞与重力。`--selftest` 会**真的走一遍**从底层到顶层的楼梯，落点必须是上一层楼面。

⛔ 消费方联动：anima-zero 的 `sim-house-nav` 已同步（新增 `HOUSENAV_SCENE`）。
老资产库没有 `scenes/manifest.py`，那边**直接报错要求升级**，不做静默退回单场景——
退回会让这个配置变成看着生效实际被忽略的东西。

## [0.5] — 2026-07-27

Main: **人形 G1 换上会原地转身的新策略**。旧策略站着几乎不转（3.8°/s），只能靠
「转弯时带 0.3 m/s 前进速度」绕过去，代价是转 90° 往前挪 0.6~0.8 米——在住宅过道和门洞
的尺度上是个实打实的能力洞。换装后清单里的 `turn_vx` 从 0.3 **降为 0.0**。

Features:

1. **`policies/g1-29dof-turn/` 换新**：`policy.onnx` 与 `contract.json` 换成 locomotion 项目
   S2-实验5「上游对齐包」的产物。屋内闭环实测（同屋同起点，命令 wz=0.8）：
   转 90° → 90.4°／2.28 s／**漂移 0.05 m**；转 180° → 180.3°／3.84 s／漂移 0.04 m。
   同口径下旧策略转 90° 需带 0.3 m/s、漂移 0.52 m，且仓内工装 `test_walk.py` 的转向测试**不通过**。
2. **契约沿用说明入库**：新旧策略的观测布局逐位相同（差异全在命令采样与奖励表），
   所以 `contract.json` 的布局字段原样有效、没有重新 dump；这件事连同证据写进契约的
   `layout_carryover` 字段，`generated_utc` 保持原值不谎报。
3. ⛔ **新增一条部署约束**：这一版策略训练时带线速度死区，**命令只能取 0 或 ≥ 0.2 m/s**，
   (0, 0.2] 这一段策略一次都没见过。现有取值（直走 0.6、转弯 0.0）都在安全区，
   日后加慢速原语时别填 0.1 这类值。已写进 `robots/manifest.py`。

## [0.4] — 2026-07-26

Main: 开源。改名 **alice-house**（Alice 是这一系列里第一号机器人，这是她的房子），
采用 MIT 许可，场景目录扁平化。

Features:

1. **MIT 开源**：顶层 `LICENSE`；仓里的宇树 Go2 / G1 模型来自 MuJoCo Menagerie，
   保留各自的 BSD-3-Clause 许可，来源与"我们改了上游什么"都已入库。
2. **改名 Domus → alice-house**，README 重写成对外口径（不再讲 ANIMA/SOMA 内部体系）。
3. **不再分场景目录**：`domus01/` 的内容全部上移到仓根。原来的设计是 `Domus01/Domus02…`
   多套户型并列，现在的方向是**一个世界里有多个地方**（房子 → 以后可能是一座岛，
   上面有房子、山路、码头），所以先扁平化，等真要加地方时再按"地方"组织。
   顺带修掉两处路径：机器人 XML 的 `meshdir` 从仓根算起，`make_house.py` 加载清单的路径同改。
4. **双语 README**（公开仓规范）：英文 `README.md` 为主展示、`README_zh.md` 为中文版，
   顶部带语言切换与状态徽章，补了目录与可直接复制运行的 Quick Start。
   原中文 README 的全部内容（户型图、实拍、设计原则、版本说明）原样保留。
5. **接入 agent-rules 规范库**：`AGENTS.md` + `CLAUDE.md` 两个极简 shim，
   固定在上游 release tag `v0.3.0`（⛔ 不跟 `main`）；本仓自己的红线（几何不手写、
   单一真相源、`pd_mode` 搞反当场倒、第三方许可不许动）与**与上游的偏离**都在 `AGENTS.md` 里明写。

⛔ 修两处过时/错误：
- `make_docs_images.py` 的输出路径在扁平化时没跟着改（还是 `../docs/images`），配图被写到
  **仓库外面**去了——脚本照常打印"完成"，README 的图却一张没更新。目录一动，所有 `..` 都要重数。
- 配图里的眼高对齐了真实机器人：狗 0.50→**0.38 m**、人形 1.55→**1.25 m**。
  1.55 那个数是还没有真人形时随手定的假机位，现在 G1 的头部相机是实打实装在 `torso_link` 上的。
- README 结尾还写着"私有仓"——已改成 MIT 与第三方许可说明。

## [0.3] — 2026-07-26

Main: 从「一只狗的场景」升级成「可以换身体的场景」——新增宇树 G1 人形（29 自由度），
场景按机器人各生成一份，机器人的全部事实收进一张清单。

Features:

1. **机器人清单**（`robots/manifest.py`）：模型在哪、出生多高、相机叫什么、力矩怎么发，
   一台机器人一条，是「这台机器人是什么」的单一真相源。加一台只往里追加。
   ⚠️ 刻意**不重复**策略契约已有的东西（关节顺序/增益/观测格式/控制周期）——那些的真相源是
   `contract.json`，抄两份必然对不上。
2. **新增 G1 人形**（`robots/g1/`）：从 MuJoCo Menagerie 导入（BSD-3-Clause，许可与上游说明入库），
   导入写成可重跑脚本 `import_from_menagerie.py`，说清楚动了上游什么——
   位置执行器换成力矩执行器（部署器自己算力矩）、加一只头部前视相机（眼高约 1.25 m、下俯 10°）。
   站立 1.38 m，同一间屋子和四足狗看到的完全不是一回事。
3. **一台机器人一份场景**：`make_house.py --robot <key>` 产出 `house-go2.xml` / `house-g1.xml`
   （机器人的网格路径在 MJCF 编译期就定死，两台塞不进同一份模型）。
   顺带去掉了场景目录下那两个 symlink（`assets` / `go2.xml`）——只能挂一台机器人，
   第二台一来就撞名；改成各机器人 XML 自己声明 meshdir。
4. **出生高度移出场景**：`layout.py` 只留"这套房子里从哪儿开始"（x/y 与朝向），
   出生高度是机器人的事（狗 0.445 m、人形 0.80 m），进清单。

⛔ 修掉一处遗留的方向性错误：`layout.py` 的家具注释里还写着 v0.2 已经改掉的
「识别特征必须铺在狗视高 0.3–0.9m」——README 改了、代码注释漏了。现已对齐成
「按真实做，不为某一种机器人定制」。人形进来之后这条尤其要紧。

## [0.2] — 2026-07-25

Main: 中厨按真实厨房重排——机器狗实测在里面转了 43 步认不出这是厨房，查出来是
12 空间合并时留下的烂摊子；顺带把配图的机位写成脚本，场景一改就能全部重出。

Features:

1. **中厨重排 + 新增家电**：鞋帽间那组 2.6m 宽、2.3m 高的整墙柜原先横在中厨正中（还堵着
   西墙那扇门），把 45㎡ 切成三块、只靠 54cm 和 90cm 两条缝连通。真实住宅不会这样——
   橱柜沿墙走、中间是通行区。改成：主操作台一字型贴西墙（洗碗机·水槽·灶台·烤箱），
   储物高柜与冰箱贴北墙，中间留 3.2m 通行区。新增**烤箱**（黑玻璃门+控制面板+横把手）与
   **洗碗机**（整扇不锈钢门），都嵌在台面下——真实厨房的装法。配两张新贴图：
   `oven_glass`（深色玻璃+斜向高光+隐约的烤架横线）、`appliance_panel`（拉丝钢+屏+指示灯）。
2. **厨房与卫生间改铺大理石**：真实住宅这两处的常见做法。新增 `marble_warm`（暖米色→中厨）
   与 `marble_grey`（冷灰→主卫/客卫）两个色号；玄关保持原来的白色大理石。
3. **配图可重跑**（`make_docs_images.py`）：机位、眼高、朝向全写进代码，一条命令重出全部。
   新增「站在门口望进中厨」「狗视角-过道」「人形视线高度看同一间中厨」三张。
   背景：第一版配图是手工摆机位截的，中厨一重排就全过时、还没人记得相机在哪。

⛔ **设计原则改掉一条方向性错误**：早期 README 写「识别特征必须铺在 0.3–0.9m 高度带」——
那是拿一台特定机器人（四足狗，相机 0.5m）的视角当设计目标。以后要上人形（1.5m+），
为狗压低家具就得全部重做。改成「**按真实做，不为某一种机器人定制**」+「摆位要真实：
沿墙布置，中间留通行区」。看不见高处是机器人侧的局限，该用环视/抬头解决。

## [0.1] — 2026-07-25

Main: 建库（当时叫 Domus）。首个场景——参照真实豪宅大平层户型图复刻的三室两厅双卫，
12 个空间 364 ㎡，供四足机器狗做室内导航与房间识别。

Features:

1. **程序化场景生成**：几何不手写，`layout.py` 一份布局定义（房间矩形/门窗/家具/窗外景物）
   经 `make_house.py` 生成 MJCF。改屋子只改布局，场景与消费方读同一真相源。
   门/通道统一登记在 `DOORS` 表里按墙匹配，支持整段拆墙（客餐一体）。
2. **参数化家具构件库**（`furniture.py`）：一件家具由五到十个零件组成——椅子有四腿和横撑、
   落地灯有配重底盘和收口、花瓶是底足-鼓腹-收颈-瓶口的真器型、圆茶几三段式。
   支持朝向旋转。共 270 件家具零件。
3. **程序化贴图 + 材质**（`make_textures.py`）：木地板（错缝板材+木纹+板缝）、瓷砖、大理石脉络、
   地毯绒毛、织物经纬、艺术漆墙面、**城市天际线**（三层楼群+空气透视+窗格灯光）、四款抽象挂画。
   配 25 种材质，金属反光/玻璃半透/镜面高反射/屏幕自发光各就各位。
4. **为低视角设计**：识别特征铺在 0.3–0.9 m（四足机器狗相机高度带）；封天花板、
   天花板单独归组便于出俯视图；窗外三层景深（树木草地→楼房→天际线背景板）。

⚠️ 已知边界（如实登记）：漫反射贴图，无法线/粗糙度贴图、无全局光照——
定位是"精致的风格化游戏场景"，不是 GTA 那种美术手工资产的水平。
