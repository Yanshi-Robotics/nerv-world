# CLAUDE.md 兼容入口

本仓的规则入口是 [AGENTS.md](AGENTS.md)。这个文件只是给认 `CLAUDE.md` 的工具留的转发。

## 读法

1. 先读 [AGENTS.md](AGENTS.md)（项目级规则 + 上游版本锚点）。
2. 再按它指的上游入口读共享规范：
   [agent-rules@v0.3.0/AGENTS.md](https://github.com/jeffliulab/agent-rules/blob/v0.3.0/AGENTS.md)。
3. 冲突时**本仓规则优先**。

⛔ 上游版本固定在 release tag（`v0.3.0`），不跟 `main`。
