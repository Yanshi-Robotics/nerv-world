### alice-house 的家具做成可交互

> 登记于 2026-08-09。
> ⭐ **2026-08-22 大幅更新**：其中「碰撞体」那一半**已经做完了**（见下面第一节），
> 顺带订正了本文原来两处**说错的事实**。铰接与操作策略仍未开工。

---

## ✅ 已完成：家具有了和真家具一样的碰撞（2026-08-22）

新场景 **apt2** 里，餐椅 / 单椅 / 茶几走 **CoACD 凸分解**做真碰撞，
沙发按 G1 腿长手写成座面 0.35 m。相关代码：`decor/hulls.py`（生成）、
`decor/lock.py` 的 hull 访问器、`make_house._hull_geoms()`（发射）、
`check_scene` 的 `check_collision_solref` 与 `check_seat_reachable`（门禁）。

**根因不是「没开碰撞」**，而是：`decor/convert.py` 按**材质**拆部件（glTF primitive
本来就按材质分组），而 MuJoCo 对每张碰撞网格只取**凸包**。两件事一叠加，凸包把空腔整个吞掉。
实测各部件凸度（实体积 ÷ 凸包体积）：餐椅 **0.30**、单椅骨架 **0.12**、圆茶几 **0.22**、
大床软包 **0.07**。⇒ 直接把外衣的 `contype` 打开没用，只是把实心盒换成实心凸包。

CoACD 阈值 0.10 的效果。⭐ 下面两组都是**从仓里两份产物直接量的**，可复现：

```
 apt1 · 单椅（一个隐身碰撞盒）        apt2 · 餐椅（17 块 CoACD 凸块）
  110  110  110  110  110  110  110    ·   94   95   95   95   94    ·   ← 靠背
  110  110  110  110  110  110  110    ·   78   87   87   86   79    ·
  110  110  110  110  110  110  110    ·   36   45   46   46   36    ·   ← 座面
  110  110  110  110  110  110  110    ·   43   45   45   45   43    ·
  110  110  110  110  110  110  110    ·   43   43   43   43   43    ·

  占地上方一律 110 cm                   靠背 94–95、座面 43–46，
  —— 0.90×1.06×1.10 的实心砖            边上直接看到地面 —— 一把真椅子
```

同一条横射线，两个场景对照：
- **座面高度 0.45 m**：apt2 撞在 `furn_dn_w1__h8` 上，0.737 m —— 椅子确实在那儿。
- **座面下方 0.26 m**：apt1 的单椅把它挡在 **0.721 m**（实心盒）；
  apt2 的餐椅让它**一路穿过去**，15.25 m 外才撞到图书室的书架。

代价可以接受，实测整套房子：**apt1 3329 geom / 15.7× 实时 → apt2 3719 geom
（含 184 个碰撞凸块）/ 14.1× 实时**，约 10%。
⭐ 而且 `nmeshgraph` 与件数**无关**（网格按资产共享），所以 6 把同款餐椅只声明一套凸块。

### ⛔ 一个必须记住的坑：`solref`

MuJoCo 默认接触**太软**。3.9 kg 的板从 1.20 m 砸到 49 mm 厚的座面凸块上会**直接穿过去**。
阈值很陡：每步位移 3.2 mm 还好、4.1 mm 就穿。
⚠️ 减小时间步没用、加 `margin` 也没用（都实测过）。只有 `solref="0.005 1"` 有效。
⛔ 不写它的后果是「慢慢坐下去一切正常、摔一跤砸上去就穿模」，而且**编译不报错、慢速测试全绿**。
现在由 `check_collision_solref` 钉着。

### ⭐ 一个意外的结论：真家具对 G1 全都太高

| | 座面高 | 判定 |
|---|---|---|
| 手写沙发（按 G1 腿长定） | **35.0 cm** | ✅ |
| Poly Haven 餐椅 | 42.3 – 46.1 cm | ❌ |
| Poly Haven 单椅 | 53.0 – 70.9 cm | ❌ |

G1 的上限是实测的 **0.36 m**（小腿 0.318 + 踝高 0.033 = 0.351；坐姿保持 3 秒：
0.30 ✅ / 0.35 ✅ / 0.40 ❌ 滑落 / 0.45 ❌ 直接倒）。
⛔ 别为了让真家具"能坐"去缩放资产——`_fit_scale` 是均匀缩放，会把整把椅子缩成玩具尺寸。
⇒ **尺寸由机器人定的那件家具，就不该让资产来定它的尺寸**，所以沙发用手写基本体。

---

## ⛔ 本文原来说错的两件事（2026-08-22 订正）

**① 「一把餐椅的碰撞体是一个 0.46×0.60×1.0 的实心长方体」——只对一半的家具成立。**
这句话对**套了真网格外衣**的件是对的（`mesh_piece()` 出的件 = 一个隐身盒 + 一件不参与碰撞
的网格外衣，apt1 里共 31 个）。但**纯基本体拼的家具早就是多盒子的**：
`furniture.chair()` 出的是座面 + 靠背 + 两根竖条 + 四腿 + 两根横撑共 **10 个独立盒子**，
apt1 的客厅沙发也本来就是 4 个盒子（座面顶在 0.42）。
⚠️ 照原话理解会以为"全仓家具都是实心砖"，从而低估已有的东西、也找错要改的地方。

**② 「RoboCasa 的 collision 组是一堆凸分解碎块」——不对。**
它们是解析的 `type="box"` / `type="cylinder"` **基本体**（在 `decor/robocasa.py` 的注释里）。
这一条很要紧：意味着接铰接时那些碰撞体**可以直接搬**，不用重新做凸分解。

**③ 顺带**：本文和 `CHANGELOG.md` 都写着「48 项自检」，实际 `CHECKS` 里现在是 **29 项**
（本轮新加两项之前是 27）。

---

## ⏳ 还没做的

**1. 铰接**：冰箱门、抽屉在 RoboCasa 原始资产里**本来是能开的**，搬进来时只取了视觉网格。
   ⭐ 好消息是**字节就在本机**：`.cache/robocasa/{fridges,stoves,sinks,hoods}.zip`
   （137 MB / 151 个模型 / CC-BY-4.0）是原生 MuJoCo MJCF，完整带着 22 个关节
   （冰箱 2 门 + 4 抽屉、灶具烤箱门 + 烤架 + 6 旋钮、水槽 3、抽油烟机 5 按钮）、
   每个 body 的基本体碰撞、以及灶具那 8 个 `burner_*_place_site`。
   而且 `decor/assets/rc_*/pN.obj` 与那些可动 body **严格一一对应**
   ⇒ **接回铰接是改元数据的活，不用重新下载、不用重新处理任何网格。**
   要改的是 `decor/robocasa.py` 的 `_body_chain()`（现在只回传 geom 和累计平移，
   body 名和关节从头到尾没接住）与那句 `"_vis" not in mn: continue`。

   ⚠️ 三个已实测的坑，动手前先看：
   - **父子 body 之间 MuJoCo 不会自动免除接触**：柜体挂 worldbody（无关节）、门是它的
     子 body（有 hinge），几何重叠会产生 4 个常驻接触（`filterparent` 不生效，因为柜体的
     `body_weldid` 就是 world）。⇒ 必须发 `<contact><exclude .../></contact>`。
   - **关节的 `range`/`ref` 带角度单位**，跟 `<compiler angle>` 走。默认 `degree` 下
     `range="0 1.5708"` 会被静默编译成 `0…0.0274 弧度`。这和 2026-08-05 那次 `euler`
     灾难是同一类错，而关节没有"用四元数绕开"这条退路 ⇒ 只能靠门禁对账。
   - ⭐ **`<joint ref="...">` 就是免关键帧的 `qpos0`**（实测 `ref="0.61"` → `qpos0[0]==0.61`）。
     ⇒ 家具的静止姿态不需要关键帧，门可以"生下来就半开"，`make_house.py` 依然不用 import mujoco。

**2. 会操作的策略**：G1 现役是平地行走 ONNX（480 维观测 / 29 维动作 / 隐式 PD /
   线速度命令有死区），**不含坐下、抓取、上楼梯**。
   ⛔ 这条线不在本仓（本仓章程「不训练策略」），归 `yanshi-rl-lab`。
   ⚠️ 两块已知的拦路石：① 那边的 editable 安装是**悬空的**（指向已不存在的
   `~/2077-Yanshi-Robotics/...`），今天根本训不了；② 它产出的是 `schema_version: 2`
   契约，而消费方 `sim.py:82` 读 `d["term_scales"]`——v2 没这个键，**当场 KeyError**。
   这两件要在训练出结果**之前**定，不然训完了才发现装不进去。

---

**⭐ 性能从来不是障碍**（2026-08-09 实测，这一条原文是对的，保留）：
把 apt1 整件家具逐个改成 `<body><freejoint/>` 之后——

| 可动件数 | nv | 常驻接触 | 实时倍率 |
|---|---|---|---|
| 0（当时的现状） | 35 | 10 | 15.7× |
| 10 | 95 | 62 | 11.7× |
| 68（全部整件家具） | 443 | 333 | **4.9×** |

整层全动仍有 4.9 倍余量，而 apt1 真正的瓶颈是那路 15 fps 直播渲染（把整体拖到 0.47 倍）
——**渲染比全部家具可动还贵三倍**。真正的成本是**常驻接触数**不是自由度。
⇒ 业界做法（RoboCasa / Habitat / Isaac）本来也不是"整层全动"，是**只让任务涉及的物体可动**。
apt2 的 `decor/hulls.py` 里那份 `COLLIDE` 清单就是照这条来的。
