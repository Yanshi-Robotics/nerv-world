# Agent working notes (local only — not part of the open-source repo).
# ⛔ AGENTS.md is NOT here on purpose: it is the public, machine-facing entry point and is tracked.
CLAUDE.md
.claude/

__pycache__/
*.pyc
.venv/
preview/
output/
.DS_Store

# Scratch + stray runtime logs. NOTE: scenes/**/output/ are deliverables and stay tracked;
# only this repo's own temp/ scratch dir and MuJoCo's stray log are ignored here.
temp/
MUJOCO_LOG.TXT
node_modules/
.pytest_cache/
decor/assets/
# 上游原始压缩包与 GeoJSON 缓存（RoboCasa zip 有 135 MB）——只是下载中转，不入库
.cache/

# 给 Jeff 看图的中转夹（正本在工作区根，见全局 CLAUDE.md）——绝不入库
临时图片拷贝版本/
